#################
##  Functions  ##
#################
# Time-domain functions for the signal-to-noise ratio.
# The backaction rates that are returned by the qubit dephasing function could be used to calculate the Hamiltonian
representing the induced frequency shift on the qubits, along with the effective collapse operators for the reduced
qubit master equation.
# Create data classes for the output of SNR and Backaction functions
# Implement time-dependent class to handle time-dependent QGstates, QGopers, QGsupers, QGhles.
# Function for stochastic master equations.
# Implement functions to model homodyne, heterodyne, generaldyne, etc measurements.


###############
##  QGstate  ##
###############
# For convenience, should it possible to switch between "Quadrature" and "Ladder" bases for the CVSS component?
# Maybe also allow for different quadrature bases to be chosen. This would require that the symplectic form now be a
a property which must be set. An argument against this is that methods like the partial trace for the CV component 
become annoying to implement since quadratures for the same mode are no longer neighbours.
# Implement magic methods. str and maybe repr. Additional methods like iter, setitem, delitem. matmul and rmatmul 
as shorthand for added operations on the tensor structure.
# Permute method to reorder FLS and CVS modes.
# Other methods/attributes to implement:
copy() Create a copy of QGstate
ptrace() Traces over selected mode indices listed in array. Only computable for purely FLS states or CVS component of states
# Features to add to multiplication
    - Multiplication of pure FLS states, equivalent to bare matrix multiplication
    - Multiplication of pure CVSS states, equivalent to Moyal product between Gaussian Wigner distributions
# Figure out how to better handle multiplying a CVS-only QGstate by negative numbers. Possible workaround is 
to not use the zeroth-order cumulant, but instead the zeroth-order moment/total mass. This may be a good reason to 
combine data_0th and data_fls.
	
	

##############
##  QGoper  ##
##############
# Allow for conversion between "Quadrature" and "Ladder" bases for the CV component?
# The ordering of quadrature operators is fixed in the current implementation, so the commutation relations always take the form:
	[r_j,r_k] = i*Ω_jk where the symplectic form is Ω = ⊗_{j=1}^N [[0,1],[-1,0]].
In order to allow for other orderings, it should be possible to submit a desired symplectic form Ω to represent alternate commutation relations.
# Permute method to reorder FLS and CVS modes.
# Implement some magic methods. str, repr, maybe iter, setitem, delitem. matmul and rmatmul as tensor shorthand.
# Confirmation: The application of the canonical commutation relation is obtained using the following:
        ½r_j*r_k*O(2)_jk 
        = ½r_j*r_k*(O(2)_jk + O(2)_jk^T)/2 + ½r_j*r_k*(O(2)_jk - O(2)_jk^T)/2 
        -> data_2nd = (O(2)_jk + O(2)_jk^T)
    And we now move to the asymmetric part, using r_j*r_k-r_k*r_j = i*Ω_jk, using the fact that the labels are 
    arbitrary and Ω_jk = -Ω_kj:
        ½r_j*r_k*(O(2)_jk - O(2)_jk^T)/2 
        = r_j*r_k*(O(2)_jk - O(2)_kj)/4 
        = (r_j*r_k*O(2)_jk - r_k*r_j*O(2)_kj - i*Ω_jk*O(2)_kj)/4 
        = -i*(Ω_jk*O(2)_kj)/4 
        = -i*(Ω_jk*(O(2)_kj-O(2)_kj^T))/8

	

###############
##  QGsuper  ##
###############
# Implement some magic methods. str, repr, maybe iter, setitem, delitem.
# Permute method to reorder FLS and CVS modes.
# Should we automatically symmetrise data_2nd_l and data_2nd_r through commutation relations?
# See if we can take a set of coefficients from a Wigner PDE and back-generate the superoperator.
# Implement iscptp (completely-positive and trace-preserving) property
# Possibly combine 2nd and 1st order coefficients into unified structures, maybe something like
	- data_2nd = [data_2nd_l,data_2nd_m,data_2nd_r]
	- data_1st = [data_1st_l,data_1st_r]
Where the correct data could be called using data_2nd(str) where str={'L','M','R'}, for example.
